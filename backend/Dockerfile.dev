FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Ensure Prisma client engine is generated for the Linux container runtime.
RUN npx prisma generate

EXPOSE 3000

CMD ["npm", "run", "dev"]
