---
description: Clearing House Phase-2 mission-level gate is live at 3
alwaysApply: true
---

# Clearing House Phase-2 gate

Runtime key: `CREW_MISSION_CLEARING_HOUSE_MIN_MISSION_LEVEL`.

**Live (2026-09-08):** prod runtime is **`3`** after explicit user enable. Code default stays **`0`**.

## Keep it on 3 unless

- User asks to turn the extra lock **off** (set runtime to `0` via Admin → Crew Missions)
- Exploit/speedup telemetry shows the gate should be paused

## Do not

- Flip the **code** default from `0` to `3` (runtime is the source of truth)
- Change the live runtime value without the user asking

Docs: `docs/module-protocols/crew-missions.md`, `docs/game-systems/CREW_MISSION_CLEARING_HOUSE_VAULT_2026-04-26.md`.
